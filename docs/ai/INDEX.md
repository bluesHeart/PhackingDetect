# AI Work Notes Index (living)

Purpose: keep “decisions, pitfalls, and checklists” indexable so we can iterate toward `docs/rubric.md` without losing context.

## High-value notes
- Extraction pitfalls: reference pages, citation-like numeric cells, parentheses semantics (SE vs t).
- Extraction diagnostics: `scripts/extraction_quality_report.py` + `analysis_*/extraction_quality_report.md` + keep-rate confound check (`analysis_*/coverage_diagnostics.md`).
- Validation checklist: tasks → labels → scoring → report → how it maps into main tables.
- Sampling frame checklist: inclusion rules, de-duplication, version mapping, audit.
